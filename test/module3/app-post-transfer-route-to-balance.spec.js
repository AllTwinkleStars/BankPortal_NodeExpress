describe('Transfer post route to balance', () => {
  it('should contain to balance @app-post-transfer-route-to-balance', () => {
    assert(typeof app === 'function', '`app` const has not been created in `app.js`.');
  });
});
